version: 2.1

executors:
  ocix:
    machine:
      image: ubuntu-1604:202004-01
    working_directory: ~/ocix

commands:
  setup_environment:
    description: Setup OCI-Cross environment variables
    steps:
      - run:
          name: Export OCI-Cross Registry, Organization and Version
          command: |
            [ -d /work ] && [ -d ~/ocix/test ] && cp -rf ~/ocix/test /work
            ./scripts/ci/ocix-env.sh >> $BASH_ENV
  build_test_ocix_image:
    description: Build & test an OCI-Cross container image
    parameters:
      image_name:
        type: string
    steps:
      - restore_cache:
          key: ocix-base-assets-{{ .Revision }}
      - setup_environment
      - run:
          name: Build << parameters.image_name >>
          no_output_timeout: 1.5h
          command: |
            [ -f ~/ocix/ocix-base.tar ] && docker load -i ~/ocix/ocix-base.tar
            make << parameters.image_name >>
            docker save --output ~/ocix/<< parameters.image_name >>.tar ${OCIX_REGISTRY}${OCIX_PORT}/${OCIX_ORG}/<< parameters.image_name >>:${OCIX_VERSION}
            docker load -i ~/ocix/<< parameters.image_name >>.tar
            make << parameters.image_name >>.test
            tar -zcvf ~/ocix/<< parameters.image_name >>-work.tar.gz ~/ocix/<< parameters.image_name >>/work
            rm -rf ~/ocix/<< parameters.image_name >>/work/*
      - store_artifacts:
          path: ~/ocix/<< parameters.image_name >>-work.tar.gz
          destination: << parameters.image_name >>-work.tar.gz
      - save_cache:
          key: << parameters.image_name >>-assets-{{ .Revision }}
          paths: ~/ocix/<< parameters.image_name >>.tar
  deploy_ocix_image:
    description: Deploy a OCI-Cross container image
    parameters:
      image_name:
        type: string
    steps:
      - restore_cache:
          key: << parameters.image_name >>-assets-{{ .Revision }}
      - setup_environment
      - deploy:
          name: Deploy << parameters.image_name >>
          command: |
            echo ${OCIX_REGISTRY}${OCIX_PORT}/${OCIX_ORG}/<< parameters.image_name >>:${OCIX_VERSION}
            if [ "${CIRCLE_BRANCH}" == "develop" ]; then
              docker load --quiet --input ~/ocix/<< parameters.image_name >>.tar
              echo "${OCIX_REGISTRY_PASSWORD}" | docker login --username ${OCIX_REGISTRY_USER} --password-stdin ${OCIX_REGISTRY}${OCIX_PORT}
              docker push ${OCIX_REGISTRY}${OCIX_PORT}/${OCIX_ORG}/<< parameters.image_name >>:${OCIX_VERSION}
            fi

references:
  workflow-settings: &workflow-settings
    filters:
      branches:
        only: develop

jobs:
  ocix-base:
    executor: ocix
    steps:
      - checkout
      - setup_environment
      - run:
          name: Build ocix-base
          command: |
            docker pull debian:10.3-slim
            make ocix-base
            make ocix-base.test
            tar -zcvf ~/ocix/ocix-base-work.tar.gz ~/ocix/ocix-base/work
            rm -rf ~/ocix/ocix-base/work/*
            docker save --output ~/ocix/ocix-base.tar ${OCIX_REGISTRY}${OCIX_PORT}/${OCIX_ORG}/ocix-base:${OCIX_VERSION}
      - store_artifacts:
          path: ~/ocix/ocix-base-work.tar.gz
          destination: ocix-base-work.tar.gz
      - save_cache:
          key: ocix-base-assets-{{ .Revision }}
          paths:
            - ~/ocix/ocix-base.tar
            - ~/ocix
      - deploy_ocix_image:
          image_name: ocix-base

  ocix:
    description: Invoke the OCI-Cross Build and Test Command
    parameters:
      image:
        description: "The OCI-Cross Container Image Name"
        type: string
    executor: ocix
    steps:
      - build_test_ocix_image:
          image_name: << parameters.image >>
      - deploy_ocix_image:
          image_name: << parameters.image >>

  deploy:
    executor: ocix
    steps:
      - deploy_ocix_image:
          image_name: "ocix-base"

workflows:
  version: 2
  build-test-deploy:
    jobs:
      - ocix-base:
          <<: *workflow-settings
      # For this ARM please use the Yocto project https://www.yocto.org
      # - ocix:
      #     name: "android-arm"
      #     image: "ocix-android-arm"
      #     requires:
      #       - ocix-base
      # For this ARM please use the Yocto project https://www.yocto.org
      # - ocix:
      #     name: "android-arm64"
      #     image: "ocix-android-arm64"
      #     requires:
      #       - ocix-base
      - ocix:
          name: "linux-arm64"
          image: "ocix-linux-arm64"
          requires:
            - ocix-base
      # # For this ARM please use the Yocto project https://www.yocto.org
      # # - ocix:
      # #     name: "linux-armv5"
      # #     image: "ocix-linux-armv5"
      # #     requires:
      # #       - ocix-base
      # # For this ARM please use the Yocto project https://www.yocto.org
      # # - ocix:
      # #     name: "linux-armv5-musl"
      # #     image: "ocix-linux-armv5-musl"
      # #     requires:
      # #       - ocix-base
      # # For this ARM please use the Yocto project https://www.yocto.org
      # # - ocix:
      # #     name: "linux-armv6"
      # #     image: "ocix-linux-armv6"
      # #     requires:
      # #       - ocix-base
      # - ocix:
      #     name: "linux-armv7"
      #     image: "ocix-linux-armv7"
      #     requires:
      #       - ocix-base
      # # For this ARM please use the Yocto project https://www.yocto.org
      # # - ocix:
      # #     name: "linux-armv7a"
      # #     image: "ocix-linux-armv7a"
      # #     requires:
      # #       - ocix-base
      # # Image build currently broken. See issue #209
      # # - ocix:
      # #    name: "linux-mipsel"
      # #    image: "ocix-linux-mipsel"
      # #    requires:
      # #      - ocix-base
      # - ocix:
      #     name: "linux-s390x"
      #     image: "ocix-linux-s390x"
      #     requires:
      #       - ocix-base
      # # Currently build is red needs investigation
      # #
      # # - ocix:
      # #     name: "linux-ppc64el"
      # #     image: "ocix-linux-ppc64el"
      # #     requires:
      # #       - ocix-base
      - ocix:
          name: "linux-x64"
          image: "ocix-linux-x64"
          requires:
            - ocix-base
      # - ocix:
      #     name: "linux-x86"
      #     image: "ocix-linux-x86"
      #     requires:
      #       - ocix-base
      # - ocix:
      #     name: "manylinux1-x64"
      #     image: "ocix-manylinux1-x64"
      #     requires:
      #       - ocix-base
      # - ocix:
      #     name: "manylinux1-x86"
      #     image: "ocix-manylinux1-x86"
      #     requires:
      #       - ocix-base
      # - ocix:
      #     name: "manylinux2010-x64"
      #     image: "ocix-manylinux2010-x64"
      #     requires:
      #       - ocix-base
      # - ocix:
      #     name: "manylinux2010-x86"
      #     image: "ocix-manylinux2010-x86"
      #     requires:
      #       - ocix-base
      # # Currently build is red needs investigation
      # #
      # # - ocix:
      # #     name: "manylinux2014-x64"
      # #     image: "ocix-manylinux2014-x64"
      # #     requires:
      # #       - ocix-base
      # # Currently build is red needs investigation
      # #
      # # - ocix:
      # #     name: "web-wasm"
      # #     image: "ocix-web-wasm"
      # #     requires:
      # #       - ocix-base
      # # Currently build is red needs investigation
      # #
      # # - ocix:
      # #     name: "windows-static-x64"
      # #     image: "ocix-windows-static-x64"
      # #     requires:
      # #       - ocix-base
      # - ocix:
      #     name: "windows-static-x64-posix"
      #     image: "ocix-windows-static-x64-posix"
      #     requires:
      #       - ocix-base
      # - ocix:
      #     name: "windows-static-x86"
      #     image: "ocix-windows-static-x86"
      #     requires:
      #       - ocix-base
      # - ocix:
      #     name: "windows-shared-x64"
      #     image: "ocix-windows-shared-x64"
      #     requires:
      #       - ocix-base
      # - ocix:
      #     name: "windows-shared-x64-posix"
      #     image: "ocix-windows-shared-x64-posix"
      #     requires:
      #       - ocix-base
      # - ocix:
      #     name: "windows-shared-x86"
      #     image: "ocix-windows-shared-x86"
      #     requires:
      #       - ocix-base
